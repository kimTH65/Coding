N,M = map(int,input().split())
a_maxLen = M - ((N-1)*10) 
que = [[i] for i in range(1,a_maxLen+1)]
while que :
    if len(que[0]) == N : 
        break
    sel = que.pop(0)
    f_len = sel[-1]
    if len(sel) >1 :
        f_len = sel[-1] - (len(sel)-1)*10
    for i in range(f_len+len(sel)*10, len(sel)*10+a_maxLen+1):
        que.append(sel+[i])
print(que)
